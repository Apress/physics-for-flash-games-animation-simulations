package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;			public class Rope extends Sprite{		public function Rope():void{			init();		}		private function init():void{			// create a sprite to draw the springs on			var spring:Sprite=new Sprite();			addChild(spring);						// create a fixed support			var fixedPoint:Ball;			fixedPoint=new Ball(2,0x000000);			fixedPoint.pos2D=new Vector2D(100,100);			addChild(fixedPoint);							// create the masses			var objects:Array = new Array();				var numObjects:Number = 15;			var spacing:Number = 20; // make close to spring length			for (var i:uint=0; i<numObjects; i++){											var object:Ball= new Ball(2,0x000000,10);				object.pos2D = new Vector2D(fixedPoint.x+spacing*(i+1),fixedPoint.y);				addChild(object);				objects.push(object);			}						// get things moving			var mover:RopeMover=new RopeMover(objects,fixedPoint,spring);			mover.startTime();		}	}}