package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;	import com.physicscodes.objects.Particle;			public class DraggingOscillations extends Sprite{		public function DraggingOscillations():void{			init();		}		private function init():void{			// create a spring			var spring:Sprite=new Sprite();			addChild(spring);						// create a fixed support			var fixedPoint:Ball;			fixedPoint=new Ball(2,0x000000);			fixedPoint.pos2D=new Vector2D(275,50);			addChild(fixedPoint);							// create the oscillating object			var object:Ball;			object = new Ball(15,0x0000ff,1);			object.pos2D = new Vector2D(275,200);			addChild(object);						// get things moving			var oscillator:DraggingOscillator=new DraggingOscillator(object,fixedPoint,spring);			oscillator.startTime(10);		}	}}