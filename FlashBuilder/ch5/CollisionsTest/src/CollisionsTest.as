package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;		import com.physicscodes.motion.Mover;		public class CollisionsTest extends Sprite{		private var _ball1:Ball = new Ball(15,0x0000ff);		private var _ball2:Ball = new Ball(15,0xff0000);		private var _ball3:Ball = new Ball(15,0x00ff00);						public function CollisionsTest():void{			init();		}		private function init():void{			setupBall(_ball1,new Vector2D(50,200),new Vector2D(30,0));					setupBall(_ball2,new Vector2D(500,200),new Vector2D(-20,0));			setupBall(_ball3,new Vector2D(300,200),new Vector2D(10,0));						var collider:Collider=new Collider([_ball1,_ball2,_ball3]);					collider.startTime(10);		}		private function setupBall(ball:Ball,pos:Vector2D,velo:Vector2D):void{				ball.pos2D = pos;			ball.velo2D = velo;						addChild(ball);					}	}}