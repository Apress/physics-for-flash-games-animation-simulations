package {	import com.physicscodes.motion.Mover;	import com.physicscodes.objects.Ball;	import com.physicscodes.objects.Wall;		import com.physicscodes.math.Vector2D;		import flash.display.Sprite;	public class WallBouncer extends Mover{		private var _ball:Ball;						private var _wall:Sprite;		public function WallBouncer(pball:Ball,pwall:Sprite):void{					_ball = pball;			_wall = pwall;			super(pball);		}									override protected function moveObject():void{			super.moveObject();			checkBounce();								}				private function checkBounce():void{			if (_ball.x > _wall.x - _ball.radius){				_ball.x = _wall.x - _ball.radius; 				_ball.vx *= -1;			}		}		}}