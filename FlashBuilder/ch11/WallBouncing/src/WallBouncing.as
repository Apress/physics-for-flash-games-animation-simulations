package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Wall;		public class WallBouncing extends Sprite{		public function WallBouncing():void{			init();		}		private function init():void{			var ball:Ball = new Ball(15,0x000000,1);			ball.pos2D = new Vector2D(100,100);			ball.velo2D = new Vector2D(500,100);			addChild(ball);				var wall:Sprite = new Sprite();			with (wall.graphics){				lineStyle(1);				moveTo(0,50);				lineTo(0,350);			}			wall.x = 400;			addChild(wall);			var bouncer:WallBouncer=new WallBouncer(ball,wall);			bouncer.startTime();				}	}}