package {	import com.physicscodes.motion.ForcerRB;	import com.physicscodes.motion.Forces;		import com.physicscodes.objects.RigidBody;			public class RigidBodyMover extends ForcerRB{		private var _body:RigidBody;				private var _kLin:Number=0.1; // linear damping factor				private var _kAng:Number=0.5; // angular damping factor		public function RigidBodyMover(pbody:RigidBody):void{			_body = pbody;						super(pbody);		}					// NB: need to specify both force and torque		override protected function calcForce():void{			force = Forces.zeroForce();			force = force.addScaled(_body.velo2D,-_kLin); // linear damping			torque = 1;			torque += -_kAng*_body.angVelo; // angular damping 			trace(_body.angVelo);		}	}}