package{	import flash.display.Sprite;	import com.physicscodes.objects.Star;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Particle;	public class Wormhole extends Sprite{		public function Wormhole():void{			init();		}		private function init():void{  					// create a stationary black hole			var blackHole:Star = new Star(20,0x222222,10000);			blackHole.pos2D = new Vector2D(400,400);									addChild(blackHole);						// create a stationary white hole			var whiteHole:Star = new Star(10,0xffffff);			whiteHole.pos2D = new Vector2D(400,100);									addChild(whiteHole);			// create an initial distribution of stars 			var numStars:uint = 1000;						var stars:Array=new Array();			for (var i:Number=0;i<numStars; i++){				var star:Star = new Star(2,0xffff00);				star.pos2D = new Vector2D(Math.random()*stage.stageWidth,Math.random()*stage.stageHeight);				addChild(star);				stars.push(star);			}						// make the stars move			var mover:WormholeMover=new WormholeMover(stars,blackHole,whiteHole);					mover.startTime();		}	}}