package{	import flash.display.Sprite;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Ball;		public class RocketTest extends Sprite{		public function RocketTest():void{			init();		}		private function init():void{			// create 100 stars randomly positioned			for (var i:uint=0; i<100; i++){				var star:Ball = new Ball(Math.random()*2,0xffff00);				star.pos2D = new Vector2D(Math.random()*stage.stageWidth,Math.random()*stage.stageHeight);				addChild(star);			}						// create a stationary planet			var planet:Ball;			planet = new Ball(100,0x0033ff,1000000);			planet.pos2D = new Vector2D(400,400);						addChild(planet);			// create a rocket						var rocket:Rocket;			rocket = new Rocket(10,15,0xcccccc,10);			rocket.pos2D = new Vector2D(400,300);				addChild(rocket);						// launch the rocket from the planet			var rocketer:Rocketer=new Rocketer(rocket,planet);					rocketer.startTime(10);		}	}}