package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;	import com.physicscodes.objects.Particle;		public class PendulumDemo extends Sprite{		public function PendulumDemo():void{			init();		}		private function init():void{			// create a cord			var cord:Sprite=new Sprite();			addChild(cord);								// pendulum length			var lengthP:Number=100;						// create a pivot			var pivot:Ball;			pivot=new Ball(2,0x000000);			pivot.pos2D=new Vector2D(275,150);			addChild(pivot)										// create a pendulum bob			var bob:Ball;			bob = new Ball(10,0x333333,1);			var initAngle:Number = 30;			var relativePos:Vector2D = new Vector2D(lengthP*Math.sin(initAngle*Math.PI/180),lengthP*Math.cos(initAngle*Math.PI/180));			bob.pos2D = pivot.pos2D.add(relativePos);			//bob.pos2D = new Vector2D(250,200);			//trace(relativePos.length);			addChild(bob);									// make the pendulum move			var pendulum:Pendulum=new Pendulum(bob,cord,pivot.pos2D);			pendulum.startTime(10);		}	}}