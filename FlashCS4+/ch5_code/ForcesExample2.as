package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;			public class ForcesExample2 extends Sprite{		public function ForcesExample2():void{			init();		}		private function init():void{			var ball:Ball;			ball = new Ball(15,0x000000,1);			ball.pos2D = new Vector2D(75,50);			addChild(ball);				var faller:Faller2=new Faller2(ball);			faller.startTime();				}	}}