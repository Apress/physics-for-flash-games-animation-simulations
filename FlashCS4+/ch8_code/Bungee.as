package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;	import com.physicscodes.objects.Particle;			public class Bungee extends Sprite{		public function Bungee():void{			init();		}		private function init():void{			// create a spring			var spring:Sprite=new Sprite();			addChild(spring);						// create a fixed support			var fixedPoint:Ball;			fixedPoint=new Ball(2,0x000000);			fixedPoint.pos2D=new Vector2D(275,50);			addChild(fixedPoint);							// create the bungee jumper			var jumper = new Jumper();			jumper.pos2D = new Vector2D(275,50);			//jumper.pos2D = new Vector2D(300,50);						jumper.mass = 90;			addChild(jumper);						// get things moving			var oscillator:BungeeOscillator = new BungeeOscillator(jumper,fixedPoint,spring);			oscillator.startTime();		}	}}