package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.motion.MultiMover;		import com.physicscodes.motion.Mover;		import flash.geom.Vector3D;			public class BallTest extends Sprite{				public function BallTest():void{			init();		}			private function init():void{/*						var ball:Ball;			ball = new Ball();			ball.pos=new Vector3D(150,50,0);			ball.velo=new Vector3D(30,20,0);			addChild(ball);			ball.move();//			var mover:Mover=new Mover(ball);//			mover.startTime();	*/					var balls:Array = new Array();			var numBalls:Number = 10;			for (var i:uint=1; i<=numBalls; i++){				var ball:Ball;				var radius:Number = (Math.random()+0.5)*20;				var color:uint = Math.random()*0xffffff;				ball = new Ball(radius,color);				ball.pos=new Vector3D(Math.random()*550,Math.random()*400,0);				ball.velo=new Vector3D((Math.random()-0.5)*100,(Math.random()-0.5)*100,0);				addChild(ball);				//ball.move();				balls.push(ball);			}						var multimover:MultiMover = new MultiMover(balls);			multimover.startTime();								}	}}