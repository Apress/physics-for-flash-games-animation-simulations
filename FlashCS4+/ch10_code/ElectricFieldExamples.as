package{	import flash.display.Sprite;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Ball;	public class ElectricFieldExamples extends Sprite{		public function ElectricFieldExamples():void{			init();		}		private function init():void{			var particle:Ball;			particle = new Ball(5,0xff0000,1,1);				particle.pos2D = new Vector2D(100,300);			addChild(particle);			var timeStep:Number = 10;			var electro:Electro=new Electro(particle);					electro.startTime(timeStep);		}	}}