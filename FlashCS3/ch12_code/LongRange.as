package{	import flash.display.Sprite;	import com.physicscodes.objects.Star;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Particle;	public class LongRange extends Sprite{		public function LongRange():void{			init();		}		private function init():void{  					// create a stationary attracting nucleus			var massNucleus:Number = 10000;					var nucleus:Star = new Star(20,0x333333,massNucleus);			nucleus.pos2D = new Vector2D(400,400);									addChild(nucleus);			// initial distribution of stars 			var stars:Array=new Array();			var numStars:uint = 50;			for (var i:Number=0;i<numStars; i++){				var star:Star = new Star(5,0xffff00,1000);				star.pos2D = new Vector2D(Math.random()*stage.stageWidth,Math.random()*stage.stageHeight);				star.velo2D = new Vector2D(50*(Math.random()-0.5),50*(Math.random()-0.5));				addChild(star);				stars.push(star);			}						// make the stars move			var mover:LongRangeMover=new LongRangeMover(stars,nucleus);					mover.startTime();		}	}}