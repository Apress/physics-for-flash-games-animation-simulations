package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.objects.Box;		import com.physicscodes.math.Vector2D;	import com.physicscodes.objects.Particle;	import flash.display.Graphics;			public class CarDemo extends Sprite{		public function CarDemo():void{			init();		}		private function init():void{							// create a circular track, e.g. a roundabout			with (graphics){				beginFill(0xcccccc);				drawCircle(275,200,100);				drawCircle(275,200,50);				endFill();			}						var center:Ball;			center=new Ball(5,0x000000);			center.pos2D=new Vector2D(275,200);			addChild(center);							// create a car			var car:Box;			car = new Box(10,20,0x0000ff,90);			car.pos2D = new Vector2D(350,200);			car.velo2D=new Vector2D(0,-20);			car.angVelo = -car.velo2D.length/(car.pos2D.subtract(center.pos2D).length);			addChild(car);							// make the car move			var mover:CarMover=new CarMover(car,center.pos2D);			mover.startTime(10);		}	}}