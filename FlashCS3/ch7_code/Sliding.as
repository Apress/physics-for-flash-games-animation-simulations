package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Particle;	public class Sliding extends Sprite{		public function Sliding():void{			init();		}		private function init():void{			// create a ball					var ball:Ball;			ball = new Ball(20,0x0000ff,1);			ball.pos2D = new Vector2D(50,130);			ball.velo2D = new Vector2D(0,0);						addChild(ball);						// create an inclined plane			var xtop:Number=50;			var ytop:Number=150;						var xbot:Number=450;			var ybot:Number=250;						var surface:Sprite = new Sprite;			with (surface.graphics) {				lineStyle(1,0x333333,1);				moveTo(xtop,ytop);				lineTo(xbot,ybot);							}			addChild(surface);									var vec:Vector2D = new Vector2D(1,0);			var vecp:Vector2D = vec.perp(1);			trace(vecp.x,vecp.y);			// make the ball move			var angle:Number = Math.atan2(ybot-ytop,xbot-xtop);			var mover:ObliqueMover = new ObliqueMover(ball,angle);					mover.startTime(10);		}	}}