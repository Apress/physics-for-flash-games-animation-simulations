package{	import flash.display.Sprite;	import com.physicscodes.objects.Ball;	import com.physicscodes.math.Vector2D;		import com.physicscodes.objects.Particle;	public class Orbits extends Sprite{		public function Orbits():void{			init();		}		private function init():void{						// create 100 stars randomly positioned			for (var i:uint=0; i<100; i++){				var star:Ball = new Ball(Math.random()*3,0xffff00);				star.pos2D = new Vector2D(Math.random()*stage.stageWidth,Math.random()*stage.stageHeight);				addChild(star);			}						// create a stationary sun			var sun:Ball;			sun = new Ball(70,0xff9900,1000000);			sun.pos2D = new Vector2D(275,200);						addChild(sun);			// create a moving planet						var planet:Ball;			planet = new Ball(10,0x0000ff,1);			planet.pos2D = new Vector2D(200,50);	//		planet.velo2D = new Vector2D(80,-40);	//		planet.velo2D = new Vector2D(85,-40);			planet.velo2D = new Vector2D(105,0);						addChild(planet);			// make the planet orbit the sun			var orbiter:Orbiter=new Orbiter(planet,sun);					orbiter.startTime(10);		}	}}